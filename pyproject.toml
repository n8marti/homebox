[build-system]
requires = ["flit_core <5"]
build-backend = "flit_core.buildapi"

[project]
name = "homebox"
description = "Manipulate MTN Homebox router"
readme = "README.md"
authors = [{name = "Nate Marti"}]
dependencies = [
    "selenium",
    "speedtest-cli-ng",
]
license = "MIT"
requires-python = ">=3.12"
dynamic = ["version"]

[project.scripts]
get-bandwidth = "homebox.cli:get_bandwidth"
toggle-apn = "homebox.cli:toggle_apn"
homebox-net-check = "homebox.cli:main"
